pipeline {
    agent any
    stages {
        stage ('BUILD MASTER1') {
            when {
                branch 'main'
            }
            steps {
                echo 'Building Production code'
            }
        
        }
        
        stage ('BUILD DEV1') {
            when {
                branch 'dev'
            }
            steps {
                echo 'Building dev code'
            }
        }
    }
}
